# Стартовый скрипт с ошибочными строками.
# Ошибочная строка выводит сообщение и пропускается,
# выполнение скрипта продолжается со следующей строки.

ls before
wat
cd "незакрытая кавычка
exit now
conf-dump extra
ls after
